{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "update-passwords": "node --env-file=../.env --import tsx ../scratch/update-passwords.ts"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "catalog:",
    "bcryptjs": "^2.4.3",
    "drizzle-orm": "catalog:",
    "tsx": "catalog:"
  }
}
