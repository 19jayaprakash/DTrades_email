{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"if (process.platform === 'win32') console.log('Windows mode')\"",
    "dev": "node --env-file=.env -e \"process.env.NODE_ENV='development'\" && pnpm run build && pnpm run start",
    "start": "pnpm -r --parallel --if-present run start",
    "start:api": "node --max-old-space-size=400 artifacts/api-server/dist/index.mjs",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:push": "pnpm --filter @workspace/db run push"
  },
  "private": true,
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.60.4",
    "prettier": "^3.8.3",
    "rollup": "^4.60.4",
    "typescript": "~5.9.3"
  }
}
